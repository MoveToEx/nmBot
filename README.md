# nmbot

## Quick start

```sh
$ cp template.env .env
$ vim .env                  # fill in environment variables
$ poetry install            # install dependencies
$ poetry shell              # start shell from venv
$ nb orm upgrade            # upgrade database to latest migration
$ nb run                    # start bot
```
